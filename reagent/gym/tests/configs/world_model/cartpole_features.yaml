env_name: CartPole-v0
model:
  WorldModel:
    trainer_param:
      hidden_size: 50
      num_hidden_layers: 2
      learning_rate: 0.005
      not_terminal_loss_weight: 1
      next_state_loss_weight: 1
      reward_loss_weight: 1
      num_gaussians: 1
num_train_transitions: 150000 # approx. 500 episodes
num_test_transitions: 6000 # approx. 30 episodes
seq_len: 1
batch_size: 1024
num_train_epochs: 10
use_gpu: false
saved_mdnrnn_path: null
